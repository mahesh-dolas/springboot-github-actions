name: Spring Boot CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Build Maven Project
        run: mvn clean verify

      - name: Get Maven Version
        run: |
          echo "VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)" >> $GITHUB_ENV

      # - name: Check Docker Username
      #   run: |
      #     echo "Docker user: ${{ secrets.DOCKER_USERNAME }}"

      # - name: Login to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}

      # - name: Build Docker Image
      #   run: |
      #     docker build \
      #       -t ${{ secrets.DOCKER_USERNAME }}/springboot-poc:${VERSION} \
      #       -t ${{ secrets.DOCKER_USERNAME }}/springboot-poc:latest .

      # - name: Push Docker Images
      #   run: |
      #     docker push ${{ secrets.DOCKER_USERNAME }}/springboot-poc:${VERSION}
      #     docker push ${{ secrets.DOCKER_USERNAME }}/springboot-poc:latest

      # - name: Display Image Tags
      #   run: |
      #     echo "Published:"
      #     echo "${{ secrets.DOCKER_USERNAME }}/springboot-poc:${VERSION}"
      #     echo "${{ secrets.DOCKER_USERNAME }}/springboot-poc:latest"